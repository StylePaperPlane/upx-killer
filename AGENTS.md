# upx-killer 开发规范

## 适用范围与优先级

本文件适用于整个仓库。用户当前明确提出的需求优先于本文；若需求会改变产品行为、发布方式或支持范围，应先明确验收条件，再修改代码。能从源码、项目配置、构建输出或官方文档查到的事实应主动查证，不把调查工作转交给用户。

需要调整开发流程或本文规则时，先阅读 [`docs/research/mattpocock-skills-development-standards.md`](docs/research/mattpocock-skills-development-standards.md)。该文档记录了从 [`mattpocock/skills`](https://github.com/mattpocock/skills) 提炼并适配到本 C++/WinUI 3 项目的依据；本文件是仓库内的执行标准和唯一规则入口。

## 产品与发布不变量

### 1. 双击 EXE 必须可以直接启动

- 发布形态保持为未打包、自包含的 WinUI 3 文件夹部署。用户从完整发布目录双击 `upx_killer.exe` 即可启动，无需安装或注册 MSIX。
- 保持 `.vcxproj` 中 `WindowsPackageType=None`、`WindowsAppSDKSelfContained=true`、`GenerateAppxPackageOnBuild=false` 和 `AppxPackage=false`。不得生成或依赖 `.msix`、`.appx` 或 `.msixbundle` 才能启动。
- “可直接启动”不等于单文件发布。EXE 所依赖的 DLL、PRI、资源和语言卫星文件必须随完整输出目录一起交付。
- 使用能在未打包模式下稳定启动的本地化方式。根 `Window` 不添加 `x:Uid`，除非已经修复并用直接启动回归证明不会重现 MRT/XAML 启动错误 `0x802B000A`。

### 2. 只支持英文和简体中文

- 应用资源仅保留英文 `en-US` 和简体中文 `zh-CN`；运行时卫星 MUI 目录仅允许 `en-us` 与 `zh-CN`。
- 新增或修改用户可见文本时，同步更新 `upx-killer/Strings/en-US/Resources.resw` 与 `upx-killer/Strings/zh-CN/Resources.resw`，不得只维护一种语言。
- 保持项目中的 PRI 资源列表、Windows App SDK MUI 过滤和构建后清理逻辑。未经用户明确同意，不得引入第三种语言或恢复其他语言包。

## 开发工作流

1. **先定义完成条件。** 用用户可观察的行为描述目标，明确范围、非目标和验证方式。只有会实质改变产品行为或范围的歧义才需要询问用户。
2. **先读再改。** 阅读相关源文件、`.vcxproj`、资源、清单、测试和既有约束；搜索已有能力与术语。不要编辑 `Generated Files`、`x64`、`.vs`、`packages` 或其他生成/构建产物来代替源配置修改。
3. **做窄而完整的纵向修改。** 每个切片都应能独立构建或验证。只实现本次验收条件需要的行为，不加入推测性抽象、无当前用途的扩展点或无关重构。
4. **保持短反馈环。** 实现过程中反复运行最快的相关构建或测试；新功能和可回归缺陷优先采用“先失败、最小实现、再通过”的测试循环。测试通过公开接缝验证可观察行为，避免绑定私有实现。
5. **按证据调试。** 先运行并记录能复现准确症状的确定性步骤，再最小化复现、列出可证伪假设、一次只改变一个变量；修复后补回归验证。编译成功不能替代真实启动、部署或语言资源检查。
6. **分别检查 Standards 与 Spec。** 收尾时独立确认代码符合仓库规范，以及改动完整且仅实现用户规格。保留用户的无关改动，不顺手扩大范围。

## 结构化与模块化开发

### 模块边界

- **按业务职责组织，而不是按技术类型堆文件。** 一个模块封装一项内聚能力，同时包含完成该能力所需的实现；不要建立只有转发、改名或逐层传参作用的浅模块。
- **每个模块只有一个面向调用者的接口。** 接口包括公开类型和函数，也包括输入输出、所有权、线程要求、不变量、错误模式及性能约束。公开头文件保持小而稳定，具体实现、第三方类型和可变细节留在模块内部。
- **优先设计深模块。** 用尽可能小的接口提供完整行为，让复杂度集中在实现内部。删除一个模块后，如果复杂度只是散回多个调用者，说明该模块有价值；如果复杂度同时消失，它通常只是多余的转发层。
- **接缝只放在确实会变化的位置。** 只有生产实现与测试替身、两个真实实现，或明确的系统边界需要替换时才引入接口和适配器；不要为每个类机械创建抽象层。
- **模块通过结果通信。** 优先接收显式依赖并返回结果，避免在内部偷偷创建全局依赖、直接操纵其他模块状态或依赖调用顺序完成隐式协议。

### 默认依赖方向

新增功能原则上按以下方向组织；规模较小时可以合并物理目录，但职责和依赖方向不能混淆：

1. **UI**：XAML、窗口状态、命令与用户交互。只负责收集输入、调用用例和呈现结果，不承载 PE/UPX 解析、文件处理或业务判断。
2. **Application**：组织一次完整用例和状态流转，例如选择文件、分析、处理、保存与错误映射；依赖 Core 的接口，不依赖具体 UI 控件。
3. **Core**：PE/UPX 领域模型、解析规则、校验和纯计算。不得依赖 XAML、窗口对象、文件选择器或其他 UI 类型。
4. **Infrastructure**：文件系统、进程、Windows API、日志和其他 I/O 适配器。它实现 Application/Core 所需的真实接缝，把平台细节留在外侧。

依赖只朝 `UI → Application → Core` 流动；`Infrastructure` 通过适配器接入由内层定义的接缝。禁止循环依赖、跨层直接访问内部实现，以及用 `Utils`、`Common`、`Helpers` 等无明确职责的目录容纳不断增长的杂项代码。

### 文件、命名与测试

- 文件、命名空间、类型和函数使用统一的领域词汇；同一概念不得在不同模块中使用不同名称。新目录必须能用一句业务职责解释，不能只表达“这里放一些类”。
- 模块的公开声明与内部实现分离。其他模块只包含公开头文件，不包含私有头文件，也不依赖生成文件或具体适配器类型。
- 测试以模块接口为测试面，验证可观察结果；内部重构不应迫使行为测试重写。纯计算直接测试，文件系统或 Windows I/O 通过真实临时资源或测试适配器验证。
- 修改跨越多个模块时，先写清受影响模块、接口变化和依赖方向，再按可独立验证的纵向切片实施；不要一次进行横跨全仓库的分层重写。

### Visual Studio Filter 分组

`.vcxproj.filters` 只负责 Solution Explorer 的虚拟分组，不代替物理目录和命名空间。新增、删除或重命名项目文件时，必须同步维护 `.vcxproj` 与 `.vcxproj.filters`，保证每个作者维护的项目项恰好归入一个职责明确的 Filter：

- `Application`：应用启动、生命周期和用例编排入口，例如 `App.xaml.*`。
- `UI\Windows\<WindowName>`：窗口的 `.xaml`、`.idl`、`.h`、`.cpp` 必须放在同一个窗口子组；其他 UI 类型按职责扩展为 `UI\Controls`、`UI\ViewModels` 等。
- `Core\<ModuleName>`：PE/UPX 模型、解析、校验和纯计算；存在实际文件时再创建对应 Filter。
- `Infrastructure\<AdapterName>`：文件系统、进程、Windows API、日志等具体适配器；存在实际文件时再创建对应 Filter。
- `Resources\Assets` 与 `Resources\Localization\en-US|zh-CN`：静态资产与两种本地化资源。
- `Build\Manifests`、`Build\Precompiled Headers`、`Dependencies`、`Documentation`、`Generated`：分别容纳清单、预编译头、依赖声明、文档和生成编译项。

Filter 使用英文职责名并保持上述层级；不创建空 Filter，不按 `.h`/`.cpp` 扩展名拆成“Header Files/Source Files”，也不把生成文件混入业务模块。

## C++ / WinUI 3 约束

- 修改作者维护的源文件和项目配置；生成代码、NuGet 包内容与构建输出不是事实源。
- 保持 C++/WinRT 与 WinUI 生命周期、线程模型和项目 XML 的有效性。涉及依赖或 Windows App SDK 行为时，先确认实际版本，再核对该版本的一手官方资料。
- 新依赖必须解决当前范围内的真实需要，并评估其对部署体积、运行时文件和直接启动能力的影响。
- 资源、启动代码、清单、Windows App SDK/NuGet 版本、复制目标或发布过滤一旦变化，必须执行下面的完整发布回归。

## 完成条件

任何会影响代码、构建、启动、部署或本地化的任务，只有同时满足以下条件才算完成：

- [ ] 每条用户验收条件都有对应实现和可复查的验证证据。
- [ ] 使用实际可用的 MSBuild 完成 `Release|x64` 构建，且为零错误；相关自动化测试或最小复现已通过。
- [ ] 从 `upx-killer/x64/Release/upx-killer/upx_killer.exe` 直接启动，不经 IDE、打包启动器或 MSIX 注册；窗口正常出现，进程不会立即退出。
- [ ] 最终输出仍包含 EXE 所需的相邻运行时文件，且不存在以安装包为启动前提的 `.msix`、`.appx` 或 `.msixbundle` 产物。
- [ ] `upx-killer/Strings` 下的源资源目录仅为 `en-US`、`zh-CN`；最终发布目录的语言卫星目录仅为 `en-us`、`zh-CN`，且两种语言资源均完整。
- [ ] 新增代码具有明确模块归属；公开接口最小，依赖方向正确，无循环依赖、跨层内部访问、UI 业务逻辑或无职责的杂项模块。
- [ ] `.vcxproj` 与 `.vcxproj.filters` 项目项同步；所有作者维护文件都归入符合职责的 Filter，XAML 相关文件成组，生成文件与业务文件分离。
- [ ] 临时日志、诊断标记和一次性原型已清理；最终差异无无关修改、重复实现或范围蔓延。
- [ ] 交付说明列出实际运行过的验证、结果、未验证项与剩余风险，不把未运行的检查写成已通过。

仅修改文档时，可跳过不受影响的构建与运行检查，但必须验证链接、路径、命令和项目事实仍然准确。
